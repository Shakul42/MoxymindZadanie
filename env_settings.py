
# When deployed to CI/CD pipeline, you should use placeholders for credentials
# and they should be replaced from CI secrets storage, local version 
# should be excluded in .gitignore file, -> not using it right now
password = "secret_sauce"
username = "standard_user"

# Similar level of extraction should be performed for URLs, Page objects 
# and repeating created functions relocated to resource files
